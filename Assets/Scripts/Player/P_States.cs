using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class P_States : MonoBehaviour // 이걸 데이터 클래스로 만들어서 status UI Controller와 연계되게 만들고
                                      // 각 스탯의 효과를 구분해서 구체화 시킨 파생 클래스를 통해서 Player의 스크립트에 영향을 미치는 함수를 만들면 되겠다. 
                                      // 클래스 구조화 ( 상속, abstuct, interface )
{
    internal void AddHealth(int val)
    {
        
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
